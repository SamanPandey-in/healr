{
  "name": "@shi/agent",
  "version": "0.1.0",
  "description": "Optional runtime agent for @shi/cdk-self-healing — X-Ray tracing and deploy event reporting.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
